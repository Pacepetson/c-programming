#include<stdio.h>
int main(){
  int num,i,item;
  printf("enter the number to be calculated");
  scanf("%d",&limit)'
  printf("\n enter the elements;",& limit);
  for(i=0,1<limit,i++){
    scanf("%d",&array[i]);
    }
    printf("enter the number to be calculated");
    scanf("%d",&item);
    for(i=0,1<limit,i++){
    if(item==array[i]){
    printf("\n the given element found at position:%d",i+1);
    }
  }
  return 0;
}  
